export type Issue = { title: string, body: string, createdAt: string, subtitle: string, image: string, number: number };
export type IssueGh = { title: string, body: string, created_at: string, labels: { name: string }[], number: number }
